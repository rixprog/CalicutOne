// Centralized frontend configuration values
// Use this `API_BASE_URL` everywhere in the frontend to avoid hardcoding the backend URL.

// Note: do not include a trailing slash so you can append paths like `${API_BASE_URL}/api/login`
export const API_BASE_URL = 'http://localhost:8000';

// Example export for other shared constants
export const TIMEOUT_MS = 15000;
